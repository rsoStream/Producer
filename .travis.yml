sudo: required

services:
  - docker

language: java
dist: trusty

env:
  - DOCKER_IMAGE=rsostream/producer:0.01

script:
  - mvn clean package -U
  - docker build -t "$DOCKER_IMAGE" .

after_success:
  - docker login -u="$DOCKER_USERNAME" -p="$DOCKER_PASSWORD"
  - docker push "$DOCKER_IMAGE"
